describe('websocket server plugin', function () {
    it('works', function () {
        // so far, this plugin is tested purely through integration with the websocket client;
        // see the tests in that package.
    })
})