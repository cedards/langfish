describe('websocket server plugin', function () {
    it('works', function () {
        // so far, this plugin is tested plurely through integration with the websocket client;
        // see the tests in that package.
    })
})